---
name: recursiveintell-final-audit
description: "Use before final Codex response: forces hostile-auditor handoff, changed files, commands run, tests, skipped checks, risks, rollback, and remaining delta."
---

Run `python3 scripts/validate_final_state.py`, `python3 scripts/check_public_claims.py`, and `bash scripts/run_rust_gates.sh` if available. Produce the required handoff.
