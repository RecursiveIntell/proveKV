#!/usr/bin/env python3
from __future__ import annotations
import json
from pathlib import Path

OUT = Path("target/turbo-quant")
OUT.mkdir(parents=True, exist_ok=True)
receipt = {
    "schema": "BenchmarkReceiptV1",
    "profile": {
        "schema": "CodecProfileV1",
        "crate": "turbo-quant",
        "crate_version": "0.2.0-alpha.1",
        "codec_kind": "TurboProd",
        "dim": 128,
        "bits": 4,
        "projections": 64,
        "seed": 42,
        "rotation_kind": "HadamardSign",
        "storage_layout": "Bitpacked",
        "qjl_enabled": True,
        "score_semantics": "InnerProductEstimate"
    },
    "corpus": {"kind": "synthetic_gaussian", "db_size": 512, "query_count": 16, "seed": 42},
    "metrics": {},
    "commands": [],
    "warnings": ["template generated by P24 helper"]
}
path = OUT / "benchmark_receipt_template.json"
path.write_text(json.dumps(receipt, indent=2) + "\n", encoding="utf-8")
print(path)
